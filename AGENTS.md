# LEX IPAN - Comprehensive System Documentation

**Версия:** 5.11 (November 2025)  
**Статус:** Production-ready  
**Технологический стек:** MUI React, HTML5, CSS3, JavaScript ES6+  
**Дата создания документа:** 15 ноября 2025

---

## 📋 Оглавление

1. [Введение](#введение)
2. [Философия проекта](#философия-проекта)
3. [Архитектура системы](#архитектура-системы)
4. [Технологический стек](#технологический-стек)
5. [Структура UI IPAN](#структура-ui-ipan)
6. [Workbench и система разработки](#workbench-и-система-разработки)
7. [Система контейнеров](#система-контейнеров)
8. [Блочная архитектура](#блочная-архитектура)
9. [Модульная система](#модульная-система)
10. [Концептуальные принципы](#концептуальные-принципы)
11. [Процессы и структуры](#процессы-и-структуры)
12. [Карточный интерфейс](#карточный-интерфейс)
13. [База данных и SSOT](#база-данных-и-ssot)
14. [Система трёх акторов](#система-трёх-акторов)
15. [Траектории развития](#траектории-развития)
16. [Документация и артефакты](#документация-и-артефакты)

---

## Введение

**LEX IPAN** (Internal Project Automation & Navigation) — это самодостаточная система автоматизации создания веб-сайтов, email-рассылок и презентаций на основе единой карточной архитектуры. Проект представляет собой интегрированную платформу для управления данными, контентом и автоматизированной генерации маркетинговых материалов.

### Ключевые характеристики

- **Самодостаточность** — работает автономно без внешних зависимостей
- **Полиморфизм** — единый интерфейс для человека и AI
- **Модульность** — система построена из независимых взаимозаменяемых модулей
- **Карточная архитектура** — универсальная система представления информации
- **MUI React** — все продукты и артефакты создаются на базе Material-UI React framework

### Области применения

1. **Веб-сайты** — автоматическая генерация сайтов из структурированных данных
2. **Email-рассылки** — персонализированные письма с карточным интерфейсом
3. **Презентации** — автоматическое создание презентационных материалов
4. **База знаний** — самодокументирующаяся система (LEX IPAN)
5. **Управление данными** — централизованное хранилище с паспортами данных

---

## Философия проекта

### Основополагающие концепции

#### 1. Bootstrap-архитектура

Система способна к самостоятельной сборке и эволюции. LEX IPAN является одновременно:
- **Документацией** — описывает саму себя
- **Продуктом** — демонстрирует свои возможности
- **Инструментом** — используется для создания новых версий

#### 2. SSOT (Single Source of Truth)

Единственный источник истины для всех данных системы:
- **База данных PostgreSQL** — центральное хранилище
- **Паспорта данных** — метаинформация о каждом наборе данных
- **Версионирование** — отслеживание изменений с checksum
- **Синхронизация** — автоматическое обновление зависимых артефактов

#### 3. Симметрия человек–ИИ

Система проектируется с учётом двух типов пользователей:
- **Человек** — визуальный интерфейс, интуитивная навигация
- **ИИ** — структурированные данные, API, машиночитаемые форматы
- **Единый контракт** — оба пользователя работают с одними данными

#### 4. Самодостаточность

Минимальная зависимость от внешних систем:
- **Standalone HTML/CSS/JS** — работает без веб-сервера
- **Встроенная конфигурация** — все настройки в одном месте
- **Автономность** — функционирует в офлайн-режиме
- **Портируемость** — легко переносится между средами

---

## Архитектура системы

### Высокоуровневая структура

```
LEX IPAN Ecosystem
│
├── UI IPAN (User Interface)
│   ├── Carcass (Каркас приложения)
│   │   ├── Named Containers (Именованные контейнеры)
│   │   ├── Areas (Области)
│   │   └── Resizers (Управление размерами)
│   │
│   ├── Workbench / Playground
│   │   ├── Isolated development space
│   │   ├── Widget prototyping
│   │   └── Module testing
│   │
│   └── Production Carcass
│       ├── Widgets (Виджеты)
│       ├── Modules (Модули)
│       └── Layouts (Компоновки)
│
├── LEX IPAN (Documentation)
│   ├── Концепции
│   ├── Структуры
│   ├── Процессы
│   ├── Представления
│   └── Глоссарий
│
├── Database Layer
│   ├── PostgreSQL
│   ├── Data Passports
│   └── SSOT enforcement
│
└── Content Generation
    ├── Websites (MUI React)
    ├── Emails (HTML/MJML)
    └── Presentations (PPTX/HTML)
```

### Компонентная модель

#### Уровень 1: Каркас (Carcass)

**Каркас** — это фундаментальная структура приложения, определяющая области размещения контента. Аналогия: скелет здания до внутренней отделки.

**Ключевые элементы:**
- **Named Containers** — именованные области с фиксированной семантикой
- **Areas** — логические зоны для размещения виджетов
- **Resizers** — границы между областями с возможностью изменения размера
- **Background** — визуальное оформление областей

#### Уровень 2: Виджеты и модули

**Виджеты** — самостоятельные UI-компоненты с чётко определённым функционалом.

**Модули** — комплексы виджетов, реализующие бизнес-логику:
- Управление контрагентами
- Управление товарами
- Система рассылок
- Генерация контента

#### Уровень 3: Контент

Данные и их представление:
- **Статьи** — структурированные текстовые материалы
- **Карточки** — унифицированные блоки информации
- **Медиа** — изображения, видео, документы

---

## Технологический стек

### Frontend

#### MUI React (Material-UI)
**Главный фреймворк** для всех продуктов IPAN:

```javascript
// Пример использования MUI компонентов
import { 
  Container, 
  Card, 
  CardContent, 
  Typography,
  Grid 
} from '@mui/material';

function IPANCard({ title, content }) {
  return (
    <Card sx={{ borderRadius: 2, boxShadow: 3 }}>
      <CardContent>
        <Typography variant="h5" component="h2">
          {title}
        </Typography>
        <Typography variant="body1">
          {content}
        </Typography>
      </CardContent>
    </Card>
  );
}
```

**Преимущества MUI:**
- Готовые адаптивные компоненты
- Единообразный дизайн
- Темизация и кастомизация
- Accessibility из коробки
- Интеграция с React экосистемой

#### HTML/CSS/JavaScript
Для статических артефактов и LEX IPAN:

**HTML5:**
- Семантическая вёрстка
- Aria-атрибуты для доступности

**CSS3:**
- CSS Custom Properties (variables)
- Flexbox и Grid Layout
- Media Queries для адаптивности

**JavaScript ES6+:**
- Модульная архитектура
- Асинхронные операции
- Fetch API для загрузки данных

### Backend

#### PostgreSQL
Центральная база данных:
- JSONB для гибких структур
- Полнотекстовый поиск
- Транзакционность
- Индексирование

#### Python
Автоматизация и обработка:
- Парсинг данных
- Генерация контента
- Интеграция с API
- Обработка файлов

### Инструменты разработки

- **Git** — версионный контроль
- **VS Code** — основная IDE
- **Figma** — дизайн и прототипирование
- **pgAdmin** — управление БД
- **Claude / ChatGPT** — AI-ассистенты для разработки

---

## Структура UI IPAN

### Концепция каркасного строительства

UI IPAN базируется на концепции **каркасного строительства**, где:

1. **Каркас (Carcass)** — несущая конструкция
2. **Области (Areas)** — зоны для размещения элементов
3. **Контейнеры (Containers)** — блоки с контентом
4. **Виджеты (Widgets)** — функциональные компоненты

### Философия разделения

Аналогия с биологией:
- **Растительная клетка** — жёсткая оболочка, статична
- **Животная клетка** — гибкая мембрана, подвижна
- **IPAN** — подвижный каркас (скелет) + гибкие элементы (органы)

### Преимущества подхода

✅ **Изолированность изменений** — правки в виджете не влияют на каркас  
✅ **Переиспользуемость** — виджеты работают в любой области каркаса  
✅ **Масштабируемость** — легко добавлять новые области и виджеты  
✅ **Тестируемость** — каждый уровень тестируется независимо

---

## Workbench и система разработки

### Что такое Workbench?

**Workbench** (также называется **Playground** в dev-среде) — это изолированное пространство для разработки и тестирования виджетов и модулей до их интеграции в production каркас.

### Архитектура Workbench

```
Production Environment          Development Environment
┌─────────────────────┐         ┌──────────────────────┐
│  Production Carcass │         │     Workbench        │
│  ┌───────────────┐  │         │  ┌───────────────┐   │
│  │   Widget A    │  │         │  │   Widget X    │   │
│  │   (stable)    │  │         │  │   (testing)   │   │
│  └───────────────┘  │         │  └───────────────┘   │
│  ┌───────────────┐  │         │  ┌───────────────┐   │
│  │   Module B    │  │         │  │   Module Y    │   │
│  │   (v2.0)      │  │         │  │   (draft)     │   │
│  └───────────────┘  │         │  └───────────────┘   │
└─────────────────────┘         └──────────────────────┘
```

### Процесс разработки

#### 1. Прототипирование в Workbench

```javascript
// workbench/widgets/NewWidget.jsx
import React from 'react';
import { Card } from '@mui/material';

export function NewWidget() {
  // Экспериментальный код
  return (
    <Card>
      <h2>New Feature (Workbench)</h2>
      {/* Тестирование новой функциональности */}
    </Card>
  );
}
```

#### 2. Тестирование в изоляции

- Проверка функциональности
- UI/UX тестирование
- Отладка багов
- Оптимизация производительности

#### 3. Рефакторинг и доработка

- Улучшение кода
- Соответствие стандартам
- Документирование
- Code review

#### 4. Миграция в Production

```bash
# Перемещение готового виджета
mv workbench/widgets/NewWidget.jsx src/widgets/
git add src/widgets/NewWidget.jsx
git commit -m "feat: add NewWidget to production"
```

### Преимущества Workbench

✅ **Безопасность** — изменения не влияют на production  
✅ **Скорость** — быстрое прототипирование без риска  
✅ **Гибкость** — свобода экспериментов  
✅ **Изоляция** — независимая разработка модулей  
✅ **Тестирование** — проверка перед интеграцией

### Конфигурация Workbench

```json
{
  "workbench": {
    "enabled": true,
    "widgets_path": "/workbench/widgets",
    "modules_path": "/workbench/modules",
    "hot_reload": true,
    "debug_mode": true,
    "isolated_state": true
  }
}
```

---

## Система контейнеров

### Трёхуровневая иерархия UIC (UI Containers)

Система UI IPAN использует трёхуровневую иерархию именованных контейнеров:

#### Уровень 1: Главные области

| Container | Назначение | Характеристики |
|-----------|-----------|----------------|
| **1left_uic** | Левая боковая панель | Resize: step, min/max width; Border: right; Scroll: нет |
| **1top_uic** | Верхняя панель | Resize: 2-step height; Border: bottom; Collapse: 0px |
| **1main_uic** | Главная рабочая область | Resize: нет; Border: нет; Основной контейнер |

#### Уровень 2: Подобласти

**В 1left_uic:**
- **2tleft_uic** — верх левой панели (border: bottom, scroll: enable)
- **2cleft_uic** — центр левой панели (scroll: vertical only on demand)
- **2bleft_uic** — низ левой панели (border: top, scroll: enable)

**В 1top_uic:**
- **2browse_uic** — панель навигации (scroll: enable)
- **2tray_uic** — панель инструментов (border: left, scroll: enable)

**В 1main_uic:**
- **2tabs_uic** — панель вкладок (border: bottom)
- **2middle_uic** — средняя панель (resize: free width, border: right)
- **2window_uic** — окно просмотра (основной контент)
- **2right_uic** — правая панель (resize: free width, border: left)

#### Уровень 3: Детализированные зоны

**В 2tabs_uic:**
- **3ltabs_uic** — левая часть вкладок (scroll: enable)
- **3rtabs_uic** — правая часть вкладок (border: left, scroll: enable)

**В 2middle_uic:**
- **3tmiddle_uic** — верх средней панели (resize: free height, scroll: vertical on demand)
- **3bmiddle_uic** — низ средней панели (flex: grow, scroll: vertical on demand)

**В 2window_uic:**
- **3tcontent_uic** — верх контента (border: bottom, scroll: enable)
- **3content_uic** — основной контент (scroll: both on demand)
- **3bcontent_uic** — подвал контента (border: top, scroll: enable)

**В 2right_uic:**
- **3taux_uic** — верх вспомогательной панели (border: bottom, scroll: enable)
- **3aux_uic** — вспомогательная панель (scroll: vertical on demand)
- **3baux_uic** — низ вспомогательной панели (border: top, scroll: enable)

### Конфигурация контейнеров

```json
{
  "areas": {
    "1left_uic": {
      "bg": "#191919",
      "size": {
        "width": "300px",
        "min_width": "60px",
        "max_width": "500px"
      },
      "resize": {
        "type": "step",
        "handle": "right"
      },
      "border": {
        "right": {
          "color": "#3d3d3d",
          "width": "1px"
        }
      },
      "scroll": false
    },
    "3content_uic": {
      "bg": "#ffffff",
      "scroll": {
        "vertical": "on_demand",
        "horizontal": "on_demand"
      },
      "flex": {
        "grow": 1
      }
    }
  }
}
```

### Типы Resize

1. **step** — дискретное изменение (свернуть/развернуть)
2. **free** — плавное изменение размера
3. **2-step** — два состояния (свернуто/развернуто)
4. **no_parameter** — размер фиксирован

### Типы Scroll

1. **enable** — всегда доступен
2. **on_demand** — появляется при переполнении
3. **only vertical on demand** — только вертикальная прокрутка
4. **vertical & horizontal on demand** — обе оси по требованию
5. **no_parameter** — прокрутка отключена

### Система границ (Borders)

Границы определяют визуальное разделение контейнеров:

```css
/* Пример CSS для границ */
.border-right {
  border-right: 1px solid var(--border-color);
}

.border-bottom {
  border-bottom: 1px solid var(--border-color);
}

:root {
  --border-color: #3d3d3d;
}
```

---

## Блочная архитектура

### Универсальная карточная архитектура

Система IPAN использует **8 базовых типов блоков** для построения любого контента:

#### 1. HERO — Главный баннер

**Назначение:** Первое впечатление, ключевое сообщение

**Структура:**
```html
<div class="hero-block">
  <div class="hero-image">
    <img src="hero_banner.png" alt="Main visual">
  </div>
  <div class="hero-content">
    <h1 class="hero-title">Заголовок</h1>
    <p class="hero-subtitle">Подзаголовок</p>
    <button class="hero-cta">Call to Action</button>
  </div>
</div>
```

**MUI React реализация:**
```jsx
import { Box, Typography, Button } from '@mui/material';

function HeroBlock({ title, subtitle, image, ctaText }) {
  return (
    <Box sx={{ 
      backgroundImage: `url(${image})`,
      minHeight: '600px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }}>
      <Box sx={{ textAlign: 'center', color: 'white' }}>
        <Typography variant="h2">{title}</Typography>
        <Typography variant="h5">{subtitle}</Typography>
        <Button variant="contained" size="large">
          {ctaText}
        </Button>
      </Box>
    </Box>
  );
}
```

#### 2. IDENTITY — Представление компании/проекта

**Назначение:** Информация «кто мы» и ценностное предложение

**Структура:**
```html
<div class="identity-block">
  <div class="identity-logo">
    <img src="logo.svg" alt="Company logo">
  </div>
  <div class="identity-text">
    <h2>О компании</h2>
    <p>Описание деятельности...</p>
  </div>
  <div class="identity-values">
    <div class="value">Ценность 1</div>
    <div class="value">Ценность 2</div>
  </div>
</div>
```

#### 3. TONES — Карточки новостей/акций

**Назначение:** Актуальные события, новости, обновления

**Типы TONES:**
1. **Success** — успехи, достижения
2. **Information** — общая информация
3. **Warning** — важные уведомления
4. **Promotion** — акции, спецпредложения
5. **Update** — обновления продукта
6. **Event** — мероприятия
7. **Announcement** — анонсы

**MUI React реализация:**
```jsx
import { Grid, Card, CardContent, Chip } from '@mui/material';

const toneColors = {
  success: '#4caf50',
  information: '#2196f3',
  warning: '#ff9800',
  promotion: '#9c27b0'
};

function TonesBlock({ tones }) {
  return (
    <Grid container spacing={3}>
      {tones.map((tone, index) => (
        <Grid item xs={12} sm={6} md={4} key={index}>
          <Card>
            <CardContent>
              <Chip 
                label={tone.type} 
                sx={{ bgcolor: toneColors[tone.type] }}
              />
              <Typography variant="h6">{tone.title}</Typography>
              <Typography variant="body2">{tone.content}</Typography>
              <Typography variant="caption">{tone.date}</Typography>
            </CardContent>
          </Card>
        </Grid>
      ))}
    </Grid>
  );
}
```

#### 4. DEPARTMENT — Разделы/подразделения

**Назначение:** Структура организации или разделы сайта

**Структура:**
```html
<div class="department-block">
  <div class="department-grid">
    <div class="department-card">
      <div class="department-icon"></div>
      <h3 class="department-title">Отдел продаж</h3>
      <p class="department-description">Описание...</p>
      <a href="#" class="department-link">Подробнее</a>
    </div>
    <!-- повторяется для каждого подразделения -->
  </div>
</div>
```

#### 5. GALLERY — Галерея товаров/проектов

**Назначение:** Визуальная презентация портфолио или каталога

**Варианты:**
- **Grid layout** — равномерная сетка
- **Masonry layout** — Pinterest-style
- **Carousel** — слайдер с прокруткой

**MUI React реализация:**
```jsx
import { ImageList, ImageListItem, ImageListItemBar } from '@mui/material';

function GalleryBlock({ items }) {
  return (
    <ImageList cols={3} gap={8}>
      {items.map((item) => (
        <ImageListItem key={item.id}>
          <img src={item.image} alt={item.title} loading="lazy" />
          <ImageListItemBar
            title={item.title}
            subtitle={item.description}
          />
        </ImageListItem>
      ))}
    </ImageList>
  );
}
```

#### 6. PRODUCTS — Товары/услуги

**Назначение:** Детальное представление предложений с ценами

**Структура:**
```html
<div class="products-block">
  <div class="product-card">
    <img src="product.jpg" class="product-image">
    <h3 class="product-name">Название товара</h3>
    <p class="product-description">Описание...</p>
    <div class="product-price">
      <span class="price-value">1000 ₽</span>
      <span class="price-unit">за шт.</span>
    </div>
    <button class="product-cta">Купить</button>
  </div>
</div>
```

#### 7. CONTACTS — Контакты и карта

**Назначение:** Информация для связи

**Компоненты:**
- Адрес
- Телефоны
- Email
- Карта (Google Maps / Yandex Maps)
- Социальные сети
- Форма обратной связи

#### 8. FOOTER — Подвал

**Назначение:** Дополнительная навигация и служебная информация

**Типовые элементы:**
- Навигация по сайту
- Юридическая информация
- Копирайт
- Соцсети
- Newsletter подписка

### Композиция блоков

Любая страница строится из комбинации блоков:

**Главная страница:**
```
1. HERO
2. IDENTITY
3. TONES (последние обновления)
4. DEPARTMENT (основные разделы)
5. PRODUCTS (топ товары)
6. CONTACTS
7. FOOTER
```

**Страница товара:**
```
1. HERO (изображение товара)
2. PRODUCTS (детальная информация)
3. GALLERY (дополнительные фото)
4. TONES (акции по товару)
5. CONTACTS
6. FOOTER
```

**Email-письмо:**
```
1. HERO (компактный баннер)
2. IDENTITY (персональное обращение)
3. TONES (2-3 новости)
4. PRODUCTS (спецпредложения)
5. CONTACTS (контактная информация)
```

---

## Модульная система

### Принципы модульности

#### 1. Независимость модулей

Каждый модуль — это самодостаточная единица:

```javascript
// Модуль контрагентов
const CounterpartiesModule = {
  state: {},
  actions: {},
  views: {},
  api: {},
  
  // Публичный интерфейс (контракт)
  interface: {
    getList: () => {},
    getById: (id) => {},
    create: (data) => {},
    update: (id, data) => {},
    delete: (id) => {}
  }
};
```

#### 2. Контракты между модулями

Модули взаимодействуют через чётко определённые интерфейсы:

```typescript
// Контракт модуля Products
interface ProductsModuleInterface {
  // Получение данных
  getProduct(id: string): Promise<Product>;
  getProducts(filters: ProductFilters): Promise<Product[]>;
  
  // Изменение данных
  createProduct(data: ProductCreate): Promise<Product>;
  updateProduct(id: string, data: ProductUpdate): Promise<Product>;
  deleteProduct(id: string): Promise<void>;
  
  // События
  onProductCreated(callback: (product: Product) => void): void;
  onProductUpdated(callback: (product: Product) => void): void;
}
```

#### 3. Внутренняя структура модуля

```
modules/
└── products/
    ├── index.ts              # Точка входа, экспорт интерфейса
    ├── state/                # Состояние модуля
    │   ├── store.ts
    │   └── actions.ts
    ├── components/           # React компоненты
    │   ├── ProductList.tsx
    │   ├── ProductCard.tsx
    │   └── ProductForm.tsx
    ├── api/                  # Работа с API
    │   └── productsAPI.ts
    ├── types/                # TypeScript типы
    │   └── product.types.ts
    └── utils/                # Утилиты модуля
        └── productUtils.ts
```

### Реестр модулей IPAN

#### Базовые модули (обязательные)

1. **CoreModule** — базовая функциональность
   - Аутентификация
   - Навигация
   - Состояние приложения

2. **UIModule** — компоненты интерфейса
   - Каркас (Carcass)
   - Виджеты
   - Темы

3. **DataModule** — работа с данными
   - API клиент
   - Кэширование
   - Синхронизация

#### Бизнес-модули

1. **CounterpartiesModule** — управление контрагентами
   - Список контрагентов
   - Карточка контрагента
   - История взаимодействий

2. **ProductsModule** — управление товарами
   - Каталог товаров
   - Карточка товара
   - Ценообразование

3. **MailingModule** — email-рассылки
   - Шаблоны писем
   - Сегментация аудитории
   - Отправка и статистика

4. **ContentModule** — управление контентом
   - Статьи
   - Медиафайлы
   - SEO

5. **ParsingModule** — парсинг данных
   - Парсеры сайтов
   - Нормализация данных
   - Импорт

#### Служебные модули

1. **LoggingModule** — логирование
2. **AnalyticsModule** — аналитика
3. **NotificationsModule** — уведомления
4. **SearchModule** — поиск

### Жизненный цикл модуля

```javascript
class IPANModule {
  constructor(config) {
    this.config = config;
    this.state = {};
    this.isInitialized = false;
  }
  
  // 1. Инициализация
  async init() {
    console.log(`Initializing ${this.config.name}...`);
    await this.loadDependencies();
    await this.setupState();
    this.isInitialized = true;
  }
  
  // 2. Загрузка зависимостей
  async loadDependencies() {
    for (const dep of this.config.dependencies) {
      await dep.init();
    }
  }
  
  // 3. Монтирование в DOM
  mount(container) {
    if (!this.isInitialized) {
      throw new Error('Module must be initialized first');
    }
    this.render(container);
  }
  
  // 4. Размонтирование
  unmount() {
    this.cleanup();
    this.state = {};
  }
  
  // 5. Уничтожение
  destroy() {
    this.unmount();
    this.isInitialized = false;
  }
}
```

---

## Концептуальные принципы

### 1. Декларация IPAN

**Миссия:** Создание самодостаточной автоматизированной системы для генерации маркетинговых материалов с единым источником данных.

**Ценности:**
- **Простота** — минимум сложности, максимум эффекта
- **Открытость** — документация доступна всем
- **Автоматизация** — машина делает рутину
- **Качество** — каждый артефакт — произведение

### 2. Самодостаточность (Self-Sufficiency)

Система способна функционировать автономно:

```
Самодостаточная система:
├── Не требует постоянного подключения к интернету
├── Работает без внешних сервисов
├── Содержит всю необходимую документацию
├── Может быть воссоздана из исходного кода
└── Способна к самообновлению
```

**Практические примеры:**
- LEX IPAN открывается как файл, работает в браузере
- Конфигурация встроена в HTML/JSON
- База данных локальная (SQLite/PostgreSQL)
- Артефакты генерируются локально

### 3. SSOT (Single Source of Truth)

**Принцип:** Одно место хранения для каждого факта.

**Реализация в IPAN:**

```sql
-- Таблица контрагентов — единственный источник данных
CREATE TABLE counterparties (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  email VARCHAR(255),
  phone VARCHAR(50),
  metadata JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Все другие сущности ссылаются на эту таблицу
CREATE TABLE projects (
  id SERIAL PRIMARY KEY,
  counterparty_id INTEGER REFERENCES counterparties(id),
  ...
);
```

**Паспорт данных:**
```json
{
  "dataset": "counterparties",
  "owner": "admin",
  "schema": {
    "table": "counterparties",
    "columns": [
      {"name": "id", "type": "integer", "primary_key": true},
      {"name": "name", "type": "string", "required": true},
      {"name": "email", "type": "string", "unique": true}
    ]
  },
  "checksum": "a3f5c8e9...",
  "last_updated": "2025-11-15T10:00:00Z",
  "version": "2.1.0"
}
```

### 4. Таксономия IPAN

**Иерархическая структура понятий:**

```
IPAN Taxonomy
│
├── Сущности (Entities)
│   ├── Counterparties (Контрагенты)
│   ├── Products (Товары)
│   ├── Articles (Статьи)
│   └── Projects (Проекты)
│
├── Структуры (Structures)
│   ├── Database Schema
│   ├── Data Passports
│   └── API Contracts
│
├── Процессы (Processes)
│   ├── Mailing (Рассылки)
│   ├── Parsing (Парсинг)
│   ├── Generation (Генерация)
│   └── Normalization (Нормализация)
│
├── Представления (Views)
│   ├── Web Sites
│   ├── Email Templates
│   ├── Presentations
│   └── LEX IPAN
│
└── Принципы (Principles)
    ├── SSOT
    ├── Self-Sufficiency
    ├── Modularity
    └── Human-AI Symmetry
```

### 5. Каталогизация и рубрикация

**Система организации контента:**

#### Рубрикаторы LEX IPAN

1. **Концепции** — философские основы
   - Декларация
   - Самодостаточность
   - Универсальная архитектура
   - SSOT
   - Таксономия
   - Каталогизация
   - Три актора
   - Модульность
   - Симметрия человек–ИИ
   - Два вектора

2. **Структуры** — технические спецификации
   - Паспорта данных
   - Таблицы и JSONB
   - Детализация таблиц и JSONB

3. **Процессы** — бизнес-логика
   - Нормализация данных поставщиков
   - IPAN Mailing

4. **Представления** — интерфейсы
   - Cards design (принципы)
   - Cards emails
   - Структура письма
   - Cards sites

5. **Sandbox** — экспериментальные разработки
   - Конструктор (интерактивный)

6. **Задачи** — текущие работы
   - Формирование локальной БД
   - Блочно-модульная вёрстка
   - Перечень TONES
   - Баннер-слайдер
   - Анимация баннера
   - Структура Products
   - Структура Counterparties

7. **Архив** — исторические материалы
   - Создание HTML версии Lex IPAN

8. **Глоссарий** — справочные материалы
   - Глоссарий терминов
   - Карта сайта

### 6. Модульность и контракты

**Принцип:** Система состоит из независимых модулей с чёткими интерфейсами.

**Контракт модуля:**
```typescript
interface ModuleContract {
  // Метаданные
  name: string;
  version: string;
  dependencies: string[];
  
  // Жизненный цикл
  init(): Promise<void>;
  destroy(): Promise<void>;
  
  // Публичный API
  interface: ModuleInterface;
  
  // События
  events: {
    onStateChange: (callback: Function) => void;
    onError: (callback: Function) => void;
  };
}
```

**Преимущества:**
- Замена модуля без влияния на систему
- Независимое тестирование
- Параллельная разработка
- Переиспользование кода

### 7. Симметрия человек–ИИ

**Концепция:** Система проектируется для двух типов пользователей одновременно.

**Для человека:**
- Визуальный интерфейс
- Интуитивная навигация
- Понятные метафоры
- Доступность (a11y)

**Для ИИ:**
- Структурированные данные (JSON, YAML)
- Машиночитаемая документация
- API endpoints
- Единая схема данных

**Пример симметрии:**

Человек видит:
```html
<div class="product-card">
  <h3>Товар: Молоток</h3>
  <p class="price">500 ₽</p>
  <button>Купить</button>
</div>
```

ИИ получает:
```json
{
  "type": "product",
  "name": "Молоток",
  "price": {
    "amount": 500,
    "currency": "RUB"
  },
  "actions": [
    {"type": "purchase", "method": "POST", "endpoint": "/api/cart"}
  ]
}
```

---

## Процессы и структуры

### 1. IPAN Mailing — Система рассылок

**Архитектура:**

```
Mailing System
│
├── Data Layer
│   ├── counterparties (БД контрагентов)
│   └── mailing_lists (сегменты)
│
├── Template Layer
│   ├── hero.html (баннер)
│   ├── identity.html (обращение)
│   ├── tones.html (новости)
│   └── footer.html (подвал)
│
├── Generation Layer
│   ├── Jinja2 templates
│   ├── Data injection
│   └── Personalization
│
└── Delivery Layer
    ├── SMTP server
    ├── Queue management
    └── Analytics
```

**Процесс рассылки:**

1. **Сегментация**
   ```sql
   -- Выборка активных клиентов
   SELECT * FROM counterparties 
   WHERE status = 'active' 
   AND email IS NOT NULL
   AND unsubscribed = false;
   ```

2. **Генерация HTML**
   ```python
   from jinja2 import Template
   
   template = Template(email_template)
   for customer in customers:
       html = template.render(
           name=customer.name,
           tones=get_recent_tones(),
           products=get_recommendations(customer.id)
       )
       send_email(customer.email, html)
   ```

3. **Персонализация**
   ```html
   <div class="greeting">
     Здравствуйте, {{ customer.name }}!
   </div>
   
   <div class="recommendations">
     {% for product in recommended_products %}
       <div class="product-card">
         <h3>{{ product.name }}</h3>
         <p>Специально для вас: {{ product.discount }}%</p>
       </div>
     {% endfor %}
   </div>
   ```

4. **Отслеживание**
   - Open rate (открытия)
   - Click rate (клики)
   - Conversions (конверсии)

### 2. Нормализация данных поставщиков

**Проблема:** Каждый поставщик предоставляет данные в своём формате.

**Решение:** Система нормализации SSN (Supplier Standard Normalization)

**Этапы нормализации:**

#### 1. Получение данных

```python
# Прайс-лист поставщика A (Excel)
supplier_a_data = pd.read_excel('supplier_a.xlsx')

# Прайс-лист поставщика B (CSV)
supplier_b_data = pd.read_csv('supplier_b.csv', sep=';')

# Прайс-лист поставщика C (JSON API)
supplier_c_data = requests.get('https://api.supplier-c.com/products').json()
```

#### 2. Маппинг полей

```python
# Конфигурация маппинга
field_mapping = {
    'supplier_a': {
        'Наименование': 'name',
        'Цена': 'price',
        'Остаток': 'stock',
        'Артикул': 'sku'
    },
    'supplier_b': {
        'Product_Name': 'name',
        'Price_RUB': 'price',
        'Quantity': 'stock',
        'Code': 'sku'
    }
}

def normalize_supplier_data(data, supplier_id):
    mapping = field_mapping[supplier_id]
    normalized = []
    
    for row in data:
        normalized_row = {
            'supplier_id': supplier_id,
            'name': row[mapping['name']],
            'price': float(row[mapping['price']]),
            'stock': int(row[mapping['stock']]),
            'sku': row[mapping['sku']],
            'updated_at': datetime.now()
        }
        normalized.append(normalized_row)
    
    return normalized
```

#### 3. Валидация

```python
from pydantic import BaseModel, validator

class ProductImport(BaseModel):
    supplier_id: str
    name: str
    price: float
    stock: int
    sku: str
    
    @validator('price')
    def price_must_be_positive(cls, v):
        if v <= 0:
            raise ValueError('Price must be positive')
        return v
    
    @validator('name')
    def name_not_empty(cls, v):
        if not v or v.strip() == '':
            raise ValueError('Name cannot be empty')
        return v
```

#### 4. Загрузка в БД

```python
def import_products(normalized_data):
    with db.transaction():
        for product in normalized_data:
            existing = db.query(Product).filter_by(
                supplier_id=product['supplier_id'],
                sku=product['sku']
            ).first()
            
            if existing:
                # Обновление
                existing.price = product['price']
                existing.stock = product['stock']
                existing.updated_at = product['updated_at']
            else:
                # Создание
                new_product = Product(**product)
                db.add(new_product)
        
        db.commit()
```

### 3. Паспорта наборов данных

**Цель:** Метаданные о каждой таблице/наборе данных

**Структура паспорта:**

```json
{
  "dataset_id": "counterparties",
  "title": "Контрагенты",
  "description": "База данных контрагентов компании",
  
  "owner": {
    "name": "Admin",
    "email": "admin@company.com",
    "role": "Data Owner"
  },
  
  "schema": {
    "table": "counterparties",
    "database": "ipan_production",
    "columns": [
      {
        "name": "id",
        "type": "SERIAL",
        "primary_key": true,
        "nullable": false,
        "description": "Уникальный идентификатор"
      },
      {
        "name": "name",
        "type": "VARCHAR(255)",
        "nullable": false,
        "indexed": true,
        "description": "Название контрагента"
      },
      {
        "name": "email",
        "type": "VARCHAR(255)",
        "unique": true,
        "nullable": true,
        "description": "Email для связи"
      }
    ],
    "indexes": [
      {"name": "idx_counterparty_name", "columns": ["name"]},
      {"name": "idx_counterparty_email", "columns": ["email"]}
    ]
  },
  
  "quality": {
    "completeness": 98.5,
    "accuracy": 99.2,
    "consistency": 100,
    "last_validated": "2025-11-15T10:00:00Z"
  },
  
  "versioning": {
    "current_version": "2.1.0",
    "checksum": "a3f5c8e9d2b1f4a7",
    "last_updated": "2025-11-15T10:00:00Z",
    "changelog": [
      {
        "version": "2.1.0",
        "date": "2025-11-15",
        "changes": ["Added email uniqueness constraint"]
      },
      {
        "version": "2.0.0",
        "date": "2025-11-01",
        "changes": ["Migration to PostgreSQL"]
      }
    ]
  },
  
  "usage": {
    "primary_uses": [
      "Email рассылки",
      "CRM",
      "Отчётность"
    ],
    "dependencies": [
      "projects",
      "orders",
      "mailing_lists"
    ]
  },
  
  "access": {
    "read": ["admin", "manager", "analyst"],
    "write": ["admin", "manager"],
    "delete": ["admin"]
  }
}
```

### 4. Таблицы и JSONB

**Гибридный подход:** Реляционные таблицы + JSONB для гибких данных

**Пример:**

```sql
CREATE TABLE products (
  -- Обязательные реляционные поля
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  sku VARCHAR(100) UNIQUE NOT NULL,
  price DECIMAL(10,2) NOT NULL,
  
  -- Гибкие атрибуты в JSONB
  attributes JSONB,
  
  -- Служебные поля
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Индекс для быстрого поиска по JSONB
CREATE INDEX idx_product_attributes ON products USING GIN (attributes);

-- Примеры запросов
-- Найти все товары с атрибутом "color": "red"
SELECT * FROM products 
WHERE attributes @> '{"color": "red"}';

-- Найти товары с весом больше 10 кг
SELECT * FROM products 
WHERE (attributes->>'weight')::numeric > 10;

-- Получить уникальные бренды
SELECT DISTINCT attributes->>'brand' as brand 
FROM products 
WHERE attributes ? 'brand';
```

**Структура JSONB для товара:**

```json
{
  "brand": "Makita",
  "country": "Япония",
  "weight": 2.5,
  "dimensions": {
    "length": 25,
    "width": 10,
    "height": 8,
    "unit": "cm"
  },
  "features": [
    "Литиевая батарея",
    "Быстрая зарядка",
    "Светодиодная подсветка"
  ],
  "warranty": {
    "period": 24,
    "unit": "months",
    "service_centers": ["Москва", "СПб"]
  },
  "certificates": [
    {"type": "CE", "number": "CE123456"},
    {"type": "EAC", "number": "EAC789012"}
  ]
}
```

**Преимущества JSONB:**
- Гибкость без изменения схемы
- Индексирование для быстрого поиска
- Валидация на уровне приложения
- Легко добавлять новые атрибуты

---

## Карточный интерфейс

### Философия карточного дизайна

**Карточка** — это атомарная единица информации с чётко определённой структурой и назначением.

### Принципы Cards Design

#### 1. Атомарность
Одна карточка = одна сущность/идея

#### 2. Самодостаточность
Карточка содержит всю необходимую информацию для понимания

#### 3. Универсальность
Одна структура для сайта, письма, презентации

#### 4. Адаптивность
Карточка трансформируется под разные размеры экрана

### Анатомия карточки

```html
<div class="ipan-card">
  <!-- 1. Визуальный якорь -->
  <div class="card-visual">
    <img src="image.jpg" alt="Visual">
  </div>
  
  <!-- 2. Заголовок -->
  <div class="card-header">
    <h3 class="card-title">Заголовок</h3>
    <span class="card-badge">Метка</span>
  </div>
  
  <!-- 3. Контент -->
  <div class="card-content">
    <p class="card-description">
      Описание...
    </p>
  </div>
  
  <!-- 4. Метаданные -->
  <div class="card-meta">
    <span class="card-date">15 ноября 2025</span>
    <span class="card-author">Автор</span>
  </div>
  
  <!-- 5. Действия -->
  <div class="card-actions">
    <button class="card-button primary">Основное действие</button>
    <button class="card-button secondary">Второстепенное</button>
  </div>
</div>
```

### MUI React реализация

```jsx
import { 
  Card, 
  CardMedia, 
  CardHeader, 
  CardContent, 
  CardActions,
  Typography,
  Button,
  Chip
} from '@mui/material';

function IPANCard({ 
  image, 
  title, 
  badge, 
  description, 
  date, 
  author,
  primaryAction,
  secondaryAction 
}) {
  return (
    <Card sx={{ 
      maxWidth: 345,
      borderRadius: 2,
      boxShadow: 3,
      transition: 'transform 0.2s',
      '&:hover': {
        transform: 'translateY(-4px)',
        boxShadow: 6
      }
    }}>
      <CardMedia
        component="img"
        height="200"
        image={image}
        alt={title}
      />
      
      <CardHeader
        title={title}
        action={
          badge && <Chip label={badge} color="primary" size="small" />
        }
        titleTypographyProps={{
          variant: 'h6',
          fontWeight: 600
        }}
      />
      
      <CardContent>
        <Typography variant="body2" color="text.secondary">
          {description}
        </Typography>
        
        <Typography 
          variant="caption" 
          color="text.secondary"
          sx={{ display: 'block', mt: 2 }}
        >
          {date} • {author}
        </Typography>
      </CardContent>
      
      <CardActions sx={{ justifyContent: 'space-between', px: 2, pb: 2 }}>
        <Button 
          variant="contained" 
          onClick={primaryAction.onClick}
        >
          {primaryAction.label}
        </Button>
        
        {secondaryAction && (
          <Button 
            variant="outlined"
            onClick={secondaryAction.onClick}
          >
            {secondaryAction.label}
          </Button>
        )}
      </CardActions>
    </Card>
  );
}
```

### Типы карточек

#### 1. Product Card — Товарная карточка

```jsx
<ProductCard
  image="/products/hammer.jpg"
  title="Молоток 500г"
  badge="Хит продаж"
  price={500}
  oldPrice={650}
  rating={4.8}
  reviews={127}
  primaryAction={{ label: "Купить", onClick: addToCart }}
  secondaryAction={{ label: "Подробнее", onClick: viewDetails }}
/>
```

#### 2. News Card — Карточка новости/tone

```jsx
<ToneCard
  type="promotion"
  image="/news/special-offer.jpg"
  title="Специальное предложение"
  badge="Акция"
  description="Скидка 20% на все товары Makita"
  date="15 ноября 2025"
  primaryAction={{ label: "Узнать больше", onClick: readMore }}
/>
```

#### 3. Contact Card — Карточка контакта

```jsx
<ContactCard
  avatar="/avatars/ivanov.jpg"
  name="Иван Иванов"
  position="Менеджер по продажам"
  email="ivanov@company.com"
  phone="+7 (999) 123-45-67"
  primaryAction={{ label: "Написать", onClick: sendEmail }}
  secondaryAction={{ label: "Позвонить", onClick: makeCall }}
/>
```

### Адаптация карточек

#### Desktop (≥1200px)
```css
.card-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 24px;
}
```

#### Tablet (768px - 1199px)
```css
@media (max-width: 1199px) {
  .card-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}
```

#### Mobile (≤767px)
```css
@media (max-width: 767px) {
  .card-grid {
    grid-template-columns: 1fr;
  }
  
  .ipan-card {
    margin-bottom: 16px;
  }
}
```

---

## База данных и SSOT

### Структура БД PostgreSQL

```sql
-- Главная схема
CREATE SCHEMA ipan;

-- Таблица контрагентов
CREATE TABLE ipan.counterparties (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  legal_name VARCHAR(255),
  inn VARCHAR(12),
  email VARCHAR(255),
  phone VARCHAR(50),
  address TEXT,
  metadata JSONB,
  status VARCHAR(50) DEFAULT 'active',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица товаров
CREATE TABLE ipan.products (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  sku VARCHAR(100) UNIQUE NOT NULL,
  category_id INTEGER,
  supplier_id INTEGER REFERENCES ipan.counterparties(id),
  price DECIMAL(10,2) NOT NULL,
  stock INTEGER DEFAULT 0,
  attributes JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица категорий
CREATE TABLE ipan.categories (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  parent_id INTEGER REFERENCES ipan.categories(id),
  slug VARCHAR(255) UNIQUE NOT NULL,
  description TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица статей
CREATE TABLE ipan.articles (
  id SERIAL PRIMARY KEY,
  title VARCHAR(500) NOT NULL,
  slug VARCHAR(500) UNIQUE NOT NULL,
  content TEXT,
  format VARCHAR(50) DEFAULT 'html',
  category VARCHAR(100),
  author VARCHAR(255),
  published_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица проектов
CREATE TABLE ipan.projects (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  counterparty_id INTEGER REFERENCES ipan.counterparties(id),
  status VARCHAR(50) DEFAULT 'active',
  metadata JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица рассылок
CREATE TABLE ipan.mailing_campaigns (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  template VARCHAR(255),
  status VARCHAR(50) DEFAULT 'draft',
  sent_at TIMESTAMP,
  recipients_count INTEGER,
  metadata JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица получателей рассылок
CREATE TABLE ipan.mailing_recipients (
  id SERIAL PRIMARY KEY,
  campaign_id INTEGER REFERENCES ipan.mailing_campaigns(id),
  counterparty_id INTEGER REFERENCES ipan.counterparties(id),
  email VARCHAR(255) NOT NULL,
  sent_at TIMESTAMP,
  opened_at TIMESTAMP,
  clicked_at TIMESTAMP,
  status VARCHAR(50) DEFAULT 'pending'
);

-- Индексы для производительности
CREATE INDEX idx_products_sku ON ipan.products(sku);
CREATE INDEX idx_products_supplier ON ipan.products(supplier_id);
CREATE INDEX idx_products_attributes ON ipan.products USING GIN(attributes);
CREATE INDEX idx_counterparties_email ON ipan.counterparties(email);
CREATE INDEX idx_articles_slug ON ipan.articles(slug);
CREATE INDEX idx_categories_slug ON ipan.categories(slug);
```

### Паспорта данных в БД

```sql
-- Таблица паспортов данных
CREATE TABLE ipan.data_passports (
  id SERIAL PRIMARY KEY,
  dataset_id VARCHAR(100) UNIQUE NOT NULL,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  owner_name VARCHAR(255),
  owner_email VARCHAR(255),
  schema_definition JSONB NOT NULL,
  quality_metrics JSONB,
  version VARCHAR(50) NOT NULL,
  checksum VARCHAR(64) NOT NULL,
  last_validated TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Вставка паспорта для counterparties
INSERT INTO ipan.data_passports (
  dataset_id, 
  title, 
  description,
  owner_name,
  schema_definition,
  version,
  checksum
) VALUES (
  'counterparties',
  'Контрагенты',
  'База данных контрагентов компании',
  'Admin',
  '{
    "table": "counterparties",
    "columns": [
      {"name": "id", "type": "SERIAL"},
      {"name": "name", "type": "VARCHAR(255)"}
    ]
  }'::jsonb,
  '2.1.0',
  'a3f5c8e9d2b1f4a7'
);
```

---

## Система трёх акторов

### Концепция Я–База–ИИ

**Три актора** системы IPAN:

```
        Я (Human)
          │
          │ читает/пишет
          ↓
        База (Database)
          ↑         ↑
    пишет│         │читает/пишет
          │         │
    Парсеры    ИИ (AI Agent)
```

#### 1. Я (Человек)

**Роль:** Стратег, контролёр, креатор

**Задачи:**
- Постановка целей
- Принятие решений
- Контроль качества
- Творческие задачи

**Инструменты:**
- UI IPAN (визуальный интерфейс)
- LEX IPAN (документация)
- Dashboards (аналитика)

#### 2. База (Database)

**Роль:** Единственный источник истины (SSOT)

**Функции:**
- Хранение данных
- Обеспечение целостности
- История изменений
- Централизация

**Характеристики:**
- PostgreSQL
- JSONB для гибкости
- Транзакционность
- Индексирование

#### 3. ИИ (AI Agent)

**Роль:** Автоматизатор, генератор, помощник

**Задачи:**
- Парсинг данных
- Генерация контента
- Аналитика
- Автоматизация рутины

**Возможности:**
- Обработка естественного языка
- Генерация HTML/CSS
- Анализ данных
- Создание документации

### Сценарии взаимодействия

#### Сценарий 1: Создание товарного сайта

```
1. Я → База: "Создать проект сайта для категории 'Инструменты'"
2. ИИ ← База: Получить список товаров категории
3. ИИ: Сгенерировать структуру сайта (React components)
4. ИИ: Сгенерировать контент (описания, SEO)
5. ИИ → База: Сохранить конфигурацию сайта
6. Я ← ИИ: Предоставить preview сайта
7. Я: Утвердить или внести правки
8. ИИ: Развернуть production версию
```

#### Сценарий 2: Email-рассылка

```
1. Я → База: "Создать рассылку для активных клиентов"
2. ИИ ← База: Получить список контрагентов (status='active')
3. ИИ ← База: Получить последние TONES
4. ИИ: Сгенерировать персонализированные письма
5. Я ← ИИ: Показать preview письма
6. Я: Утвердить
7. ИИ: Отправить рассылку
8. ИИ → База: Сохранить статистику (открытия, клики)
```

#### Сценарий 3: Парсинг прайс-листа

```
1. Парсер: Скачать прайс-лист от поставщика
2. Парсер: Нормализовать данные
3. Парсер → База: Загрузить товары
4. ИИ ← База: Обнаружить изменения цен
5. ИИ: Проанализировать тренды
6. Я ← ИИ: Отправить отчёт об изменениях
```

---

## Траектории развития

### Два вектора IPAN

#### A.IPAN — Инструментарий

**Фокус:** Внутренние инструменты для автоматизации

**Компоненты:**
- UI IPAN (интерфейс управления)
- Workbench (среда разработки)
- Парсеры и интеграции
- Генераторы контента
- Аналитика

**Приоритеты:**
1. Стабильность и надёжность
2. Производительность
3. Масштабируемость
4. Документированность

#### B.IPAN — Продукты

**Фокус:** Коммерческие проекты для клиентов

**Типы продуктов:**
- Товарные сайты (MUI React)
- Landing pages
- Email-кампании
- Презентации
- Каталоги

**Приоритеты:**
1. Качество дизайна
2. Конверсия
3. SEO-оптимизация
4. Скорость разработки

### Roadmap развития

#### Q4 2025

**A.IPAN:**
- ✅ Стабильный каркас (Carcass v2.7)
- ✅ LEX IPAN v5.11
- ⏳ Workbench/Playground
- ⏳ Модули: Counterparties, Products

**B.IPAN:**
- ✅ Dionysos (товарный сайт)
- ⏳ Шаблоны для 3 индустрий
- ⏳ Email templates

**База данных:**
- ✅ PostgreSQL схема
- ⏳ ≥500 товаров
- ⏳ ≥100 контрагентов

#### Q1 2026

**A.IPAN:**
- ⏳ Модуль аналитики
- ⏳ Система нотификаций
- ⏳ Advanced search

**B.IPAN:**
- ⏳ 5+ товарных сайтов
- ⏳ CMS для клиентов
- ⏳ White-label решение

**Автоматизация:**
- ⏳ 10+ работающих парсеров
- ⏳ Автогенерация описаний (AI)
- ⏳ Автоматические рассылки

#### Q2-Q4 2026

**Масштабирование:**
- ⏳ 20+ клиентских проектов
- ⏳ 5000+ товаров в БД
- ⏳ 1000+ контрагентов

**Новые направления:**
- ⏳ Mobile apps (React Native)
- ⏳ Marketplace интеграции
- ⏳ AI-ассистент для контента

---

## Документация и артефакты

### Структура документации

```
Documentation/
│
├── README.md                   # Обзор проекта
├── AGENTS.md                   # Этот файл (comprehensive guide)
├── HANDOFF.md                  # Передача между чатами
│
├── architecture/
│   ├── carcass.md             # Архитектура каркаса
│   ├── modules.md             # Модульная система
│   ├── database.md            # Схема БД
│   └── api.md                 # API спецификация
│
├── guides/
│   ├── getting-started.md     # Быстрый старт
│   ├── development.md         # Разработка
│   ├── deployment.md          # Развёртывание
│   └── troubleshooting.md     # Решение проблем
│
├── concepts/
│   ├── ssot.md                # Single Source of Truth
│   ├── modularity.md          # Модульность
│   ├── cards.md               # Карточный интерфейс
│   └── three-actors.md        # Я–База–ИИ
│
└── reference/
    ├── components.md          # Справочник компонентов
    ├── api-reference.md       # API reference
    ├── glossary.md            # Глоссарий
    └── changelog.md           # История изменений
```

### LEX IPAN как живая документация

**LEX IPAN** — это:

1. **Документация проекта**
   - Концепции и принципы
   - Технические спецификации
   - Примеры использования

2. **Демонстрация возможностей**
   - Карточный интерфейс
   - Блочная вёрстка
   - Адаптивный дизайн

3. **Инструмент разработки**
   - Sandbox для экспериментов
   - Примеры кода
   - Шаблоны

4. **База знаний**
   - Глоссарий терминов
   - Best practices
   - Решения проблем

### Генерация артефактов

#### Автоматическое создание сайта

```python
from ipan.generators import SiteGenerator
from ipan.database import Database

# Подключение к БД
db = Database('postgresql://localhost/ipan')

# Создание генератора
generator = SiteGenerator(
    framework='mui_react',
    theme='modern',
    language='ru'
)

# Получение данных
products = db.query('SELECT * FROM products WHERE category_id = ?', [5])
company = db.get_setting('company_info')

# Генерация сайта
site = generator.create_site(
    template='product_catalog',
    data={
        'company': company,
        'products': products,
        'blocks': [
            {'type': 'HERO', 'config': {...}},
            {'type': 'IDENTITY', 'config': {...}},
            {'type': 'GALLERY', 'config': {...}}
        ]
    }
)

# Сохранение
site.save('/output/site/')
print(f"Site generated: {site.url}")
```

#### Генерация email-рассылки

```python
from ipan.mailing import MailingCampaign

# Создание кампании
campaign = MailingCampaign(
    name='November Promotion',
    template='promotional',
    segment='active_customers'
)

# Добавление блоков
campaign.add_block('HERO', {
    'image': '/assets/promo-banner.jpg',
    'title': 'Скидка 20% на всё!',
    'subtitle': 'Только до конца ноября'
})

campaign.add_block('TONES', {
    'items': db.get_recent_tones(limit=3)
})

campaign.add_block('PRODUCTS', {
    'items': db.get_featured_products(limit=6)
})

# Отправка
results = campaign.send()
print(f"Sent: {results.sent}, Opened: {results.opened}")
```

---

## Заключение

**LEX IPAN** — это комплексная система автоматизации создания маркетинговых материалов, основанная на принципах:

✅ **Самодостаточности** — работает автономно  
✅ **Модульности** — гибкая архитектура  
✅ **Универсальности** — единый подход к разным медиа  
✅ **SSOT** — единственный источник истины  
✅ **Симметрии** — удобна и для человека, и для ИИ

### Ключевые достижения

- 📐 **Каркасная архитектура** с именованными контейнерами
- 🧩 **Блочная система** (8 типов блоков)
- 🎨 **MUI React** как основа всех продуктов
- 🛠️ **Workbench** для безопасной разработки
- 📊 **PostgreSQL + JSONB** для данных
- 🤖 **Модель трёх акторов** (Я–База–ИИ)
- 📚 **Самодокументация** через LEX IPAN

### Следующие шаги

1. **Завершить Workbench/Playground**
2. **Создать 5+ модулей для UI IPAN**
3. **Наполнить БД (1000+ товаров)**
4. **Запустить 5+ коммерческих проектов**
5. **Автоматизировать генерацию контента**

---

**Дата документа:** 15 ноября 2025  
**Версия:** 1.0.0  
**Автор:** IPAN Team  
**Лицензия:** Proprietary

**Контакты:**  
- Email: admin@ipan-project.com
- Website: https://lex-ipan.com
- Repository: https://github.com/ipan-project

---

> "Система, которая документирует сама себя, развивается быстрее, чем система, требующая внешней документации."  
> — Принцип самодостаточности IPAN

---

## Приложения

### Приложение A: Конфигурация каркаса

См. файл `carcass.config.json`

### Приложение B: Структура UI контейнеров

См. файл `config_parameters.xlsx`

### Приложение C: Примеры кода

См. репозиторий: `/examples/`

### Приложение D: API документация

См. файл `API_REFERENCE.md`

### Приложение E: Глоссарий терминов

См. `/articles/glossary.html` в LEX IPAN

---

**Конец документа**
